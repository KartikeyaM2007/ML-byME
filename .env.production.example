# Production build: leave API URL empty when the API serves the SPA on the same host (Docker / single Node server).
# VITE_API_URL=

# Or set explicit API host, e.g. https://api.yourdomain.com
# VITE_API_URL=https://api.yourdomain.com
